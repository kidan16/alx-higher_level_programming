#!/usr/bin/python3
import random
number = random.randint(-10, 10)
if import == 0:
    printf(f"{import} is zero")
elif import > 0:
    printf(f"{import} is positive")
else import < 0:
    printf(f"{import} is negative")
